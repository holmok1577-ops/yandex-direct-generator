# Яндекс Директ - Генератор рекламных объявлений

FastAPI приложение для автоматической генерации рекламных объявлений для Яндекс Директ с использованием Yandex Cloud AI.

## Возможности

- 🚀 Генерация уникальных рекламных объявлений (3 варианта)
- 🎨 Создание промо-изображений через Yandex ART
- 📊 Экспорт результатов в CSV формат
- 🎯 Определение УТП (уникальных торговых предложений) и целевой аудитории
- 🌐 Веб-интерфейс для удобной работы

## Технологический стек

- **Backend:** FastAPI, Python 3.8+
- **AI:** Yandex Cloud GPT (текст) и Yandex ART (изображения)
- **Frontend:** HTML, Jinja2 Templates
- **Deployment:** Uvicorn

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd яндекс-директ
```

### 2. Создание виртуального окружения

```bash
python -m venv .venv
```

### 3. Активация виртуального окружения

**Windows:**
```bash
.venv\Scripts\activate
```

**Linux/Mac:**
```bash
source .venv/bin/activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 5. Настройка переменных окружения

Для удобства настройки в проекте есть файл-шаблон `.env.example`. Скопируйте его в `.env` и заполните своими данными:

```bash
cp .env.example .env
```

Или создайте файл `.env` в корне проекта со следующими параметрами:

```env
YOUR_API_KEY="ваш_api_ключ_от_yandex_cloud"
YOUR_FOLDER_ID="id_вашей_папки_в_yandex_cloud"
YOUR_YANDEX_GPT_URL="https://llm.api.cloud.yandex.net/foundationModels/v1/completion"
YOUR_YANDEX_ART_URL="https://llm.api.cloud.yandex.net/foundationModels/v1/imageGeneration"
```

**⚠️ Важно:** Файл `.env` содержит секретные данные и добавлен в `.gitignore`. Никогда не загружайте его в репозиторий!

#### Как получить API ключ и Folder ID:

1. Перейдите в [консоль Yandex Cloud](https://console.cloud.yandex.ru/)
2. Создайте аккаунт или войдите в существующий
3. Создайте новый каталог (folder) и скопируйте его ID
4. В сервисных аккаунтах создайте новый сервисный аккаунт с ролью `ai.editor`
5. Создайте API ключ для этого аккаунта

### 6. Запуск приложения

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Или используя Python напрямую:
```bash
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 7. Доступ к приложению

Откройте в браузере: `http://localhost:8000`

## Структура проекта

```
яндекс-директ/
├── main.py              # Основной файл приложения
├── requirements.txt     # Зависимости Python
├── .env.example         # Шаблон переменных окружения
├── .env                 # Переменные окружения (создается из .env.example)
├── .gitignore           # Исключения для Git
├── templates/          # HTML шаблоны
│   └── index.html      # Главная страница
├── static/             # Статические файлы (CSS, JS)
├── images/             # Сгенерированные изображения
├── generated_csv/      # Экспортированные CSV файлы
└── README.md           # Этот файл
```

## Использование

1. Откройте веб-интерфейс по адресу `http://localhost:8000`
2. Введите название товара и URL посадочной страницы
3. Нажмите "Сгенерировать объявления"
4. Получите 3 варианта рекламных объявлений с изображениями
5. Скачайте результаты в CSV формате

## API эндпоинты

- `GET /` - Главная страница с формой
- `POST /generate` - Генерация 3 вариантов объявлений
- `POST /generate_one` - Генерация одного варианта объявления
- `GET /download/{file_path}` - Скачивание CSV файла

## Особенности

- **Умная генерация:** Использует Yandex GPT для создания уникальных текстов
- **Изображения:** Автоматически генерирует промо-картинки через Yandex ART
- **Вариативность:** Создает 3 разных варианта с разными УТП и подходами
- **Эвристики:** Включает запасные варианты при недоступности AI
- **Валидация:** Проверяет длину заголовков (≤81 символ) и текста (≤150 символов)

## Требования

- Python 3.8+
- API ключ Yandex Cloud
- Интернет соединение

## Лицензия

MIT License
